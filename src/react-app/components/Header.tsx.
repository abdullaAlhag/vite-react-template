//This component includes a responsive navigation bar with a smooth dropdown menu.
    // src/react-app/components/Header.tsx
    import { useState } from "react";
import { Link } from "react-router-dom";

export default function Header() {
    const [isOpen, setIsOpen] = useState(false);

    return (
        <nav className="bg-white/80 backdrop-blur-md sticky top-0 z-50 shadow-sm border-b border-gray-100 transition-all duration-300">
            <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div className="flex justify-between h-20 items-center">

                    {/* Logo */}
                    <Link to="/" className="flex items-center group">
                       {/* <div className="w-15 h-10 bg-blue-600 rounded-xl flex items-center justify-center text-white font-bold text-2xl shadow-lg shadow-blue-200 group-hover:rotate-12 transition-transform">*/}
                          {/*  TCG*/}
                        {/*</div>*/}
                      {/*  <span className="ml-3 text-xl font-black text-gray-800 tracking-tight">Techno<span className="text-blue-600">Germany</span></span> */}
                    
			  <img src="/tgclogo.jpg"  className="w-full h-40 object-cover rounded-xl mb-2 group-hover:shadow-md transition" />			
</Link>

                    {/* Desktop Navigation */}
                    <div className="hidden md:flex items-center space-x-8">
                        <Link to="/" className="text-gray-600 hover:text-blue-600 font-medium transition-colors">Home</Link>

                        {/* Dropdown Menu */}
                        <div className="relative group">
                            <button className="flex items-center text-gray-600 group-hover:text-blue-600 font-medium py-2 outline-none">
                                Categories
                                <svg className="w-4 h-4 ml-1 transition-transform group-hover:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M19 9l-7 7-7-7"></path>
                                </svg>
                            </button>

                            {/* Dropdown Content with Animation */}
                            <div className="absolute left-0 mt-0 w-56 bg-white rounded-2xl shadow-xl border border-gray-100 opacity-0 invisible group-hover:opacity-100 group-hover:visible group-hover:mt-2 transition-all duration-300 transform origin-top-left scale-95 group-hover:scale-100 overflow-hidden">
                                <div className="py-2">
                                    <Link to="/category/radiology" className="block px-4 py-3 text-sm text-gray-700 hover:bg-blue-50 hover:text-blue-600 transition">Radiology Devices</Link>
                                    <Link to="/category/laboratory" className="block px-4 py-3 text-sm text-gray-700 hover:bg-blue-50 hover:text-blue-600 transition">Laboratory Equipment</Link>
                                    <Link to="/category/dental" className="block px-4 py-3 text-sm text-gray-700 hover:bg-blue-50 hover:text-blue-600 transition">Dental Tools</Link>
                                </div>
                            </div>
                        </div>

                        <Link to="/about" className="text-gray-600 hover:text-blue-600 font-medium transition">About Us</Link>
                        <Link to="/contact" className="bg-blue-600 text-white px-6 py-2.5 rounded-full font-bold hover:bg-blue-700 hover:shadow-lg shadow-blue-200 transition-all active:scale-95">Contact Us</Link>
                    </div>

                    {/* Mobile Menu Button */}
                    <div className="md:hidden">
                        <button onClick={() => setIsOpen(!isOpen)} className="text-gray-600 p-2 outline-none">
                            {isOpen ? "✕" : "☰"}
                        </button>
                    </div>
                </div>
            </div>

            {/* Mobile Menu Dropdown */}
            <div className={`md:hidden bg-white border-b transition-all duration-300 overflow-hidden ${isOpen ? 'max-h-64' : 'max-h-0'}`}>
                <div className="px-4 pt-2 pb-6 space-y-2">
                    <Link to="/" className="block py-2 text-gray-600 font-medium">Home</Link>
                    <Link to="/about" className="block py-2 text-gray-600 font-medium">About Us</Link>
                    <Link to="/contact" className="block py-2 text-blue-600 font-bold">Contact Us</Link>
                    <Link to="/admin" className="block py-2 text-blue-600 font-bold">Login</Link>
                </div>
            </div>
        </nav>
    );
}
